# Optional basket viewer configuration.
MCPBASKET_PORT=4377
MCPBASKET_STORE_PATH=.mcpbasket/basket.json
# Listener bind address. Keep the local default unless a deliberate LAN service is required.
MCPBASKET_BIND_HOST=127.0.0.1
